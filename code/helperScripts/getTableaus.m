% not sure if this is called anywhere, but I'm too scared to delete the
% function :0

function tableaus = getTableaus()
    % Load pre-designed tableaus for the experiment
    load('tableaus.mat', 'tableaus');
end